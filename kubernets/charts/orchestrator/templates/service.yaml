apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-orchestrator.fullname" . }}
  labels:
    {{- include "argus-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-orchestrator.selectorLabels" . | nindent 4 }}
