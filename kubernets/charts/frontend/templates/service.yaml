apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-frontend.fullname" . }}
  labels:
    {{- include "argus-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-frontend.selectorLabels" . | nindent 4 }}
