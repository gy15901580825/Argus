apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-testing-api-service.fullname" . }}
  labels:
    {{- include "argus-testing-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-testing-api-service.selectorLabels" . | nindent 4 }}
