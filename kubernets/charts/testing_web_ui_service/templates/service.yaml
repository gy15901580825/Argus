apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-testing-web-ui-service.fullname" . }}
  labels:
    {{- include "argus-testing-web-ui-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-testing-web-ui-service.selectorLabels" . | nindent 4 }}
