apiVersion: v1
kind: Service
metadata:
  name: {{ include "argus-demo-target.fullname" . }}
  labels:
    {{- include "argus-demo-target.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argus-demo-target.selectorLabels" . | nindent 4 }}
