fastapi>=0.115
uvicorn[standard]>=0.30
# The demo agent walks the 402 handshake over real HTTP when it is not handed a
# test client. Unit tests inject one, so this path only shows up in a container.
httpx>=0.27
